import logoIcon from "../../../assets/header/logo/logo.svg";

const Logo = () => {
  return (
    <div className="header__logo flexCenter">
      <img src={logoIcon} alt="лого" />
    </div>
  );
};

export default Logo;
